package FunctionalInterface;

@FunctionalInterface
public interface A {
    // If an interface has only one abstract method then it is a FUNCTIONAL INTERFACE.
    // This interface will be useful for LAMBDA EXPRESSIONS.
    void myMethod();

}
